services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: stock-mlops-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
